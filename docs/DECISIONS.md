# Décisions actées

Source de vérité du projet. Format : date — décision — pourquoi.

## Produit & scope

- **2026-07-23 — VideoCuelist est un produit distinct de Lanterne.** Lanterne (repo `toolbox`) = node/player multimédia généraliste ; VideoCuelist = **régie de spectacle centrée cuelist** (scènes configurées → rappelées par cues, avec transitions). On réutilise les acquis, on ne fusionne pas les produits. *Pourquoi : demande explicite de Pym (« un outil spécifique spectacle cette fois ») ; les deux outils n'ont pas le même utilisateur au même moment (installation autonome vs régisseur en jeu).*
- **2026-07-23 — Cibles : Windows, macOS, Ubuntu, Raspberry Pi 4/5.** macOS est un ajout par rapport à Lanterne → à dérisquer en phase 0. Pi 3 exclu. *Pourquoi : parc de Pym + usage régie sur laptop Mac/Windows fréquent en spectacle.*
- **2026-07-23 — Modèle spatial : plusieurs slices par sortie ET plusieurs sorties si besoin.** Un show = N outputs, chaque output porte ses slices, chaque slice reçoit un contenu (vidéo ou matériau). *Pourquoi : demande explicite ; c'est le modèle MadMapper que Pym pratique déjà.*
- **2026-07-23 — « IFS » = ISF (Interactive Shader Format).** Vérifié dans le projet local « Materiaux IFS » : c'est du ISF (DomePack `.fs` + index.json, format MadMapper/Resolume). Support natif ISF + GLSL brut ; le DomePack de Pym doit se charger tel quel. *Pourquoi : réutilisation directe de sa bibliothèque existante + accès aux milliers de shaders d'editor.isf.video ; les inputs déclarés en JSON s'exposent automatiquement au MIDI/OSC/DMX.*
- **2026-07-23 — Licence : MIT.** *Pourquoi : cohérent avec Lanterne ; permet l'emprunt de code entre les deux repos (même auteur). Conséquence identique : aucune copie de code GPL (HPlayer2/3, VLink…), patterns seulement.*

## Stack technique

- **2026-07-23 — Rust + GStreamer + rendu OpenGL/GLES custom, comme Lanterne.** Réutilisation ciblée des crates/du code de `toolbox` : `artnet`, patterns `control-osc`/`control-midi`, trait `PlayerBackend` + `MemoryBackend`, shaders homographie/gains RGB/mires, bus de commandes, validation des chemins, écritures atomiques. *Pourquoi : décisions déjà instruites (état de l'art + audit dans Toolbox/docs/research), code testé, asymétrie décodage Pi 4/Pi 5 déjà traitée par GStreamer ; repartir de zéro en C++ jetterait tout ça.*
- **2026-07-23 — UI : web UI embarquée (axum + WebSocket), rendu vidéo natif plein écran.** L'éditeur ET la conduite tournent dans le navigateur (localhost ou distant) ; le moteur affiche les sorties en natif. *Pourquoi : (1) même UI moderne sur les 4 plateformes ; (2) contrôle à distance gratuit — éditer sur laptop un player Pi, GO depuis une tablette ; (3) « interface sexy + infobulles partout » bien plus atteignable en HTML/CSS qu'en egui/ImGui ; (4) c'est l'architecture Lanterne, déjà éprouvée. Fallback assumé : si la latence ou le canvas de mapping posent problème, panneau natif egui pour l'éditeur de mapping uniquement.*
- **2026-07-23 — Codecs : HAP recommandé pour le show, H.264/HEVC acceptés** (décodage matériel par plateforme, asymétrie Pi 4 = H.264 HW / Pi 5 = HEVC HW héritée des décisions Lanterne). Outil d'import avec conversion ffmpeg « Optimiser pour le spectacle ». *Pourquoi : HAP = décodage quasi gratuit GPU, scrub instantané, multi-couches — standard VJ ; mais gourmand en débit disque (SSD USB3 sur Pi).*

## Modèle de conduite

- **2026-07-23 — Une cue = snapshot complet de la scène** (contenus par slice, valeurs des paramètres scénarisés, réglages de lecture, transition d'entrée). Pas de tracking façon GrandMA en v1 ; cues partielles/overlays envisagées en v2. *Pourquoi : simple, prévisible, correspond au modèle mental « scène » de Pym.*
- **2026-07-23 — Moteur de transitions en double scène (decks A/B).** La cue active vit en A, la suivante se précharge en B ; une transition = blend A→B (cut, crossfade, fondu par le noir). *Pourquoi : modèle des mélangeurs vidéo — rend le préchargement et la preview naturels.*
- **2026-07-23 — Continuité de lecture : si le même média reste sur le même slice d'une cue à l'autre, la lecture ne redémarre pas** (seuls les paramètres fondent). *Pourquoi : essentiel pour des enchaînements propres en spectacle.*
- **2026-07-23 — Tout paramètre passe par un système unifié** : adresse OSC stable, MIDI learn avec soft-takeover, patch Art-Net 8/16 bits avec lissage (le DMX arrive à ~44 Hz), courbes de réponse, mode « live » (override non écrasé par les cues) vs « scénarisé ». *Pourquoi : une feature = un paramètre = automatiquement pilotable partout et enregistrable en cue — c'est la colonne vertébrale du produit.*

## Réponses de Pym du 2026-07-23 (soir)

- **2026-07-23 — Sorties : 4 simultanées maximum.** Résolution 1080p par défaut, 4K selon les vidéoprojecteurs — le moteur doit tenir 4×1080p confortablement, 4K à valider au bench par machine.
- **2026-07-23 — Audio : AUCUNE sortie audio.** À la place, un module de **modulation** de qualité : générateurs de signaux basse fréquence (LFO sinus/triangle/carré/dent de scie/random S&H — fréquence en Hz ou synchro BPM, phase, profondeur, lissage) + **analyse audio d'entrée** (FFT fenêtrée Hann, bandes log réglables, enveloppes attack/release) pour faire évoluer n'importe quel paramètre sur des fréquences définies proprement. *Interprétation de « module de synthétisation du son » = synthèse de signaux de contrôle + audio-réactif, puisque pas de sortie son — à reconfirmer avec Pym.*
- **2026-07-23 — Timecode : option utile confirmée** (chase MTC/LTC) — backlog v2, l'architecture des déclencheurs de cue doit le prévoir.
- **2026-07-23 — MIDI : mapping 100 % générique + learn.** Aucune personnalité de console spécifique : « les consoles vont changer ».
- **2026-07-23 — Partage de crates avec Lanterne : copie documentée** (en-tête de provenance dans chaque fichier repris). *Pourquoi : deux produits qui divergent, un seul mainteneur — une dépendance git créerait un enfer de versions croisées ; la copie est simple, fiable, maintenable. Décision demandée à Claude par Pym.*
- **2026-07-23 — Backend vidéo v1 : ffmpeg en sous-processus** (frames brutes par pipe, HAP/H.264/HEVC décodés par le ffmpeg full build), derrière le trait `PlayerBackend`. *Pourquoi : pas de GStreamer sur la machine de dev, ffmpeg déjà présent et embarquable dans le dossier portable, zéro problème de linkage. GStreamer reste le backend visé pour la phase Raspberry Pi.*
- **2026-07-23 — Rendu : OpenGL 3.3 (glow + winit/glutin).** *Pourquoi : ISF = GLSL compilé nativement par le driver (pas de traduction hasardeuse), shaders Lanterne repris tels quels, multi-fenêtres pour 4 sorties.*
- **2026-07-23 — Distribution : dossier portable d'abord.** Binaire + web UI embarquée + `config.toml` + dossiers `media/ shows/ shaders/ logs/` relatifs à l'exécutable. Aucune installation requise.

## Organisation

- **2026-07-23 — Convention de rangement héritée de Toolbox** : le cadrage vit dans `Programe Cue Video/` (dossier de travail), le code dans le repo `videocuelist/` (GitHub : pymenvert/videocuelist). Docs normatives : DECISIONS / SPEC / ARCHITECTURE / PLAN.
- **2026-07-23 — Chaque phase du PLAN livre un outil utilisable en l'état** avec un critère de sortie testable. *Pourquoi : de la valeur à chaque étape, correction de cap possible.*

- **2026-07-23 — Nom proposé : « Conduite »** (à valider par Pym). Recherche de collisions menée sur 7 candidats (Filage, Conduite, Servante, Poursuite, Girandole, Luciole, CueLight) : Conduite gagne — c'est littéralement le document de régie que le logiciel incarne, aucun logiciel du spectacle ne porte ce nom, handle GitHub libre, duo cohérent avec Lanterne. Réserves connues : SEO (permis de conduire) → communiquer « Conduite app », lecture anglophone « Conduit ». Dauphins : Filage (très bon mais voisin de « Filmage », apps vidéo Mac), Poursuite. À éviter : CueLight (produit commercial NuDelta du même secteur), Luciole (logiciel vidéo libre français existant).

## Assemblage `app` (binaire conduite)

- **2026-07-23 — Un seul contexte GL, une surface par fenêtre de sortie** (au lieu de contextes partagés via `with_sharing`). *Pourquoi : les VAO et FBO du compositor ne sont PAS partagés entre contextes GL (seuls textures/buffers le sont) — un contexte unique rendu tour à tour sur chaque surface (`make_current` par fenêtre) partage tout sans piège. Vsync posé sur la première surface uniquement, conforme à INTERFACES.*
- **2026-07-23 — Ajout à `core` des commandes `Undo`/`Redo` et `MidiLearnStart`/`MidiLearnCancel`** (JSON figé par test, retouche core explicitement autorisée par la mission). *Pourquoi : l'undo est une pile de snapshots côté app mais doit être pilotable par l'UI/raccourcis via le vocabulaire commun ; le learn MIDI est déclenché depuis la page Patch.*
- **2026-07-23 — DBO implémenté comme fondu maître dédié dans la session** (niveau 0..1 poussé dans `OutputView::dbo`), distinct de `CuePanic` (qui reste le noir de conduite du moteur de cues). *Pourquoi : `DboRelease` doit relever le voile sans toucher à la conduite, alors que le panic du moteur n'est relâché que par un GO.*
- **2026-07-23 — Entrée audio (cpal + rustfft) : stub propre en v1 de l'app** — `FftFrame::empty()` chaque tick, warn au démarrage si `audio_input` est configuré. *Pourquoi : les LFO fonctionnent sans ; l'intégration cpal/FFT (thread dédié, Hann 2048/hop 1024, triple buffer) reste à brancher sans changer l'architecture.*
- **2026-07-23 — Édition des cues en cours de conduite : `CueEngine::load` puis re-standby de l'ancienne cue active.** *Pourquoi : le moteur repart proprement de zéro au rechargement ; on repositionne la standby pour que le GO suivant reprenne où on en était (l'édition lourde se fait en mode Edit).*

## Réponses de Pym du 2026-07-24 (matin)

- **2026-07-24 — Nom VALIDÉ : « Conduite ».** Le repo GitHub reste `videocuelist` pour l'instant (renommage en `conduite` possible côté GitHub, les redirections sont automatiques — action de Pym s'il le souhaite).
- **2026-07-24 — Modulation : modèle Resolume adopté.** Chaque paramètre peut recevoir une source d'animation : **Timeline** (LFO/courbes), **BPM sync**, **FFT** (audio-réactif avec **choix libre de la ou des bandes de fréquences** en Hz, sélection visuelle sur un analyseur de spectre), **Timecode** (prévu, grisé jusqu'à la v2). L'UI est param-centrée (popover d'animation sur chaque paramètre) au-dessus du modèle modulateurs/routes existant.
- **2026-07-24 — Synthèse du son : PAS d'hébergement VST.** Demandé par Pym (« un VST, ça marcherait ? »), instruit et écarté : un plugin tiers dans le process peut planter le show (contraire à la doctrine §10), l'écosystème VST est quasi inexistant sur Pi/ARM, et le scanning/licences/GUI est une usine à gaz. À la place : (1) moteur interne enrichi (bandes FFT sélectionnables, LFO, BPM — puis séquenceur de pas et enveloppes en v2) ; (2) pattern pro pour les besoins extrêmes : un outil externe (VCV Rack, Ableton/Max, Chataigne) module Conduite en OSC/MIDI — la « qualité VST » tourne dehors, la fiabilité reste dedans. Si un jour nécessaire : hôte CLAP **hors process** sandboxé, v3+.
- **2026-07-24 — Sorties : 4 max, 1080p garanti sur les quatre ; 4K par sortie = objectif.** Chemin technique 4×4K : décodage HAP direct vers textures GPU compressées (sans pipe RGBA — le pipe ffmpeg à 4K = ~1 Go/s par flux) puis décodage matériel GStreamer (phase Pi). Bench par machine avant toute promesse en salle.

- **2026-07-24 — Audio-réactif « à la Resolume » consolidé côté moteur (crate `modulation`).** (1) `spectrum_bins` : réduction pure de la trame FFT en n bins log-échelonnés (défaut UI 64, 20 Hz→16 kHz), max par intervalle + compression sqrt, sortie 0..1 — alimente `dyn.fft.bins` de la trame WS. (2) `AudioBand.normalize` (serde default `true`) : AGC lent — division du niveau brut par son maximum glissant (τ ≈ 3 s, montée instantanée), garde anti-silence pour ne jamais amplifier le bruit de fond. (3) Convention BPM documentée sur `Freq::BpmSync` : `mult` = cycles par temps (1 mesure 4/4 = 0.25, 4 mesures = 0.0625). (4) `ModKind::TimecodeChase {}` réservé (types seuls) : moteur → 0.0 + `trace!`, UI grisée, champs futurs sous `serde(default)`. *Pourquoi : modèle Resolume acté le 2026-07-24 ; l'AGC rend l'audio-réactif exploitable quel que soit le gain d'entrée sans retoucher chaque bande en balance ; le variant réservé évite une rupture de format de show en v2.*
- **2026-07-24 — Entrée audio RÉELLE branchée (stub retiré) : `app::audio` (cpal 0.15 + rustfft + ringbuf + arc-swap).** Callback cpal (mixdown mono) → ring buffer lock-free → thread `conduite-audio` (Hann 2048, hop 1024, sinusoïde pleine échelle ≈ 1.0) → `ArcSwap<FftFrame>` lu sans verrou par le tick. Échec d'ouverture ou device débranché = warn (une fois par cause, debug ensuite) + re-tentative toutes les 5 s — un micro USB branché en cours de show finit par marcher. Sélection : `ShowSettings.audio_input` (nouveau champ, `serde(default)`, modifiable à chaud par `SettingsUpdate` — re-spawn propre du thread, join, zéro fuite) prime sur `config.toml/audio_input` ; `"default"` = entrée par défaut de l'OS. Trame WS `dyn` enrichie conformément au contrat : `fft:{bins:[64 lissés attack 40 ms/release 350 ms, arrondis 3 décimales], device}` **absent** si pas de capture active ; `runtime.audio_devices = {available, active}` pour l'onglet Réglages. *Pourquoi : l'audio-réactif acté le 2026-07-24 exige une FFT réelle ; le champ dans les réglages du show le rend pilotable par l'UI avec le vocabulaire `SettingsUpdate` existant, le repli config.toml préserve le réglage machine.*

- **2026-07-23 — FFmpeg embarqué : build officiel BtbN win64 LGPL (n8.1), le build GPL gyan.dev « full » est abandonné.** Vérifié : décodeurs `hap`/`h264`/`hevc` présents (libsnappy inclus), décodage réel d'une frame du HAP de démo + H.264 + aller-retour HEVC. Binaires dans `tools/ffmpeg-lgpl/` (gitignoré, traçabilité dans `VERSION.txt`), `package.ps1` refuse tout build `--enable-gpl` sans `-AllowGpl`, notices dans `licenses/FFMPEG.txt` + `LICENSE-FFMPEG-LGPLv3.txt`. *Pourquoi : le portable redistribuait un binaire GPLv3 sans notice ni offre de source — bloquant pour vendre ; la variante LGPL réduit les obligations à « texte de licence + lien source », élimine la surface brevets des encodeurs (x264/x265 exclus) et divise la taille par ~3.*
- **2026-07-23 — Attribution des dépendances : cargo-about (génération `licenses/THIRD-PARTY-NOTICES.html`, config `about.toml`) + cargo-deny en CI (`deny.toml` : allowlist MIT/Apache/BSD/ISC/Zlib/MPL-2.0/BSL/CC0/Unicode, advisories RUSTSEC, sources).** Aucun copyleft détecté dans l'arbre actuel ; seule exception documentée : advisory « unmaintained » ttf-parser (transitif winit, informatif). *Pourquoi : MIT/BSD/Apache exigent la notice de copyright en redistribution binaire ; la CI bloque toute dépendance copyleft ou vulnérable future.*

- **2026-07-24 — Chemin d'upload vidéo premium (crates engine + compositor), par capacité décroissante avec replis systématiques.** (1) **BGRA de bout en bout** sur GL desktop : le compositor active `engine::set_decode_bgra` à l'init si `GL_BGRA` est accepté (toujours en desktop, jamais en GLES — absent du cœur ES 3.0) ; ffmpeg sort `-pix_fmt bgra`, l'upload passe `GL_BGRA` (format natif Windows, zéro swizzle driver). Chaque frame PORTE son ordre de canaux (`FrameRgba::pixel_order`) : aucune course possible au changement de réglage. (2) **`glTexStorage2D`** (stockage immuable) pour toutes les textures persistantes (slices, FBO matériaux/préview) quand GL ≥ 4.2/ARB/ES 3.0 — anti-fragmentation driver sur 8 h ; changement de dimensions = texture recréée. (3) **PBO persistant mappé** (`ARB_buffer_storage`, `MAP_PERSISTENT|COHERENT`) : ring de 3 tranches par deck avec une fence par tranche (test à timeout 0, puis attentes bornées 2 ms × 8) ; replis : orphaning historique, puis copie synchrone. (4) **Latence bornée** : une fence par rendu vers une fenêtre de sortie, > 2 rendus en vol ⇒ attente courte du plus ancien (pattern mpv) ; métrique `Compositor::frames_in_flight()` pour le HUD santé. *Pourquoi : items perf de l'audit 2026-07-24 ; chaque capacité est détectée à l'init (`caps_from`, pur et testé) pour garder GL 3.3/GLES 3.0 (Raspberry Pi) fonctionnels.*
- **2026-07-24 — Décodage matériel D3D11VA (Windows) : tenté pour H.264/HEVC ≥ ~720p, repli logiciel mémorisé par session.** `-hwaccel d3d11va` (sortie logicielle des frames, le pipe rawvideo ne change pas) est ajouté au spawn ffmpeg si : Windows, codec `h264`/`hevc` (ffprobe), ET surface ≥ 1280×720 — sous ce seuil le décodage logiciel est trivial et l'init du device D3D11 (~0,1-0,3 s À CHAQUE spawn, donc à chaque seek/boucle in/out) coûterait plus qu'elle ne rapporte (constaté : 20/30 frames livrées sur un clip de 1 s en 64×64). Si le process meurt prématurément avec hwaccel : désactivation POUR LA SESSION (log une fois) + relance logicielle immédiate SANS consommer la relance « santé ». *Pourquoi : −60-80 % CPU sur H.264/HEVC 4K (audit P1) sans jamais dégrader la fiabilité — le pire cas est le comportement actuel.*

- **2026-07-24 — Fiabilité conduite (P0 audit, crates app/core/cue/control-http/media-library).** (1) **Codes de sortie normalisés** : 0 = arrêt normal, 1 = erreur générique, 2 = usage CLI, 10 = port web pris OU instance déjà lancée (le watchdog ne relance PAS), 11 = perte GPU (relance conseillée < 5 s) ; échec de bind du port web = sortie immédiate code 10, plus jamais de moteur zombie qui ouvre MIDI/autosave sans UI. (2) **`Command::Quit` + Ctrl-C** (SetConsoleCtrlHandler / SIGINT-SIGTERM) = arrêt propre : sauvegarde si modifié, Drop exécutés (flush logs, join workers), code 0. (3) **TDR/perte GPU** : contexte demandé `Robustness::RobustLoseContextOnReset` (repli standard), `glGetGraphicsResetStatus` par frame si l'extension existe, sinon détection d'échecs GL continus (≥ 120 frames ET ≥ 2 s) ⇒ log + sauvegarde synchrone + exit(11) ; warns par-frame de gfx (make_current/swap) throttlés à 1/s avec compteur de lignes supprimées. (4) **Moniteurs** : topologie surveillée (poll 2 s), fenêtre plein écran mémorise son moniteur par NOM+POSITION — perdu ⇒ repli fenêtré + warning `runtime.warnings` (action « output »), retrouvé ⇒ plein écran ré-appliqué. (5) **Anti-veille** `SetThreadExecutionState(ES_CONTINUOUS|SYSTEM|DISPLAY)` tant qu'une sortie est active. (6) **Anti double-GO** : `ShowSettings.min_go_interval_ms` (défaut 300, 0 = off) appliqué dans la session pour TOUTES les sources ; refus = `StateEvent::Warning` throttlé 1/s. (7) **`Cue.armed`** (serde default true) : désarmée = sautée par GO/BACK/follow ; GOTO et `goto_after` (cibles explicites) la jouent quand même. (8) **Undo** : coalescing des édits continus par (type, cible) avec débounce 500 ms (un drag = un snapshot) + plafond de la pile à 64 Mo estimés par sérialisation (cible Pi). (9) **Récupération** : `StateEvent::RecoveryAvailable{path,timestamp}` au démarrage si un `shows/recover-*.json` est plus récent que le show + `runtime.recovery` persistant ; `Command::RecoveryLoad` (chemin strictement validé) / `RecoveryDismiss` ; sauvegarde synchrone avant ShowLoad/ShowNew. *Pourquoi : garde-fous P0 de l'audit 2026-07-24 — chaque item correspond à une panne réelle constatée ou à une convention QLab.*
- **2026-07-24 — Observabilité runtime (contrats inter-agents).** `runtime.protocols = {osc_in, osc_out, artnet, midi}` avec statut RÉEL (`"ok" | "inactif" | "erreur: <msg>"`) — un bind OSC raté n'affiche plus jamais « port 9000 actif » ; `runtime.shows` = dossiers de `shows/` (rafraîchi 10 s en mode Edit seulement) ; `runtime.warnings = [{level, msg, action?}]` (médias manquants → « relink », MIDI → « midi », moniteur perdu → « output ») ; les WARN/ERROR de démarrage sont retenus (ring 100) et REJOUÉS à chaque connexion WS (le journal web voit les erreurs antérieures à l'ouverture de la page) ; `GET /health = {status: ok|stalled (tick > 2 s), tick_age_ms, version}` branché sur l'âge réel du tick. Logs fichiers BORNÉS : `logs/conduite.<date>.log`, `max_log_files(30)` — vérifié en réel depuis un dossier portable copié (écriture + flush à l'arrêt). *Pourquoi : P0 n°7 (état réel des protocoles), P1 logs/health de l'audit.*
- **2026-07-24 — Confort moteur (P1/P2).** Gabarits de cue `ShowSettings.cue_defaults` (transition/follow/couleur, appliqués à `CueAdd` seulement sur les champs restés aux défauts de type) ; relocalisation en cascade (`media_library::relocate_cascade` : un média retrouvé reconnecte tous les manquants du même dossier et sous-dossiers) ; pacing Windows `timeBeginPeriod(1)` (garde, relâchée à l'arrêt) + MMCSS « Pro Audio » sur le thread de rendu (dégradation silencieuse) ; hot-reload des shaders (watcher `notify` sur `shaders/`, débounce 150 ms, write-temp+rename friendly, branché sur `comp_reload` existant, IGNORÉ en mode Show) ; ré-ancrage des horloges de conduite sur saut de tick > 3 s (veille machine : plus de rafale de cues au réveil) ; purge de `thumbs/` au chargement de show (ids u32 recouverts entre shows) ; handler WS en `try_send` (un bus plein ne gèle plus la web UI). *Pourquoi : items P1/P2 de l'audit dans le périmètre moteur.*

## Lot P2 du 2026-07-28

- **2026-07-28 — Préview H.264 via ffmpeg `h264_mf` (MediaFoundation), repli MJPEG systématique.** Nouveau flux WS `GET /preview.h264` : premier message JSON `{"codec":"avc1.42E01E","format":"annexb","width","height","fps"}` puis frames binaires Annex-B ; l'encodeur est le `h264_mf` du build FFmpeg LGPL déjà embarqué (encodeur MediaFoundation Windows — pas de x264 GPL), alimenté en rawvideo par le chemin préview existant. Si `h264_mf` est indisponible sur la machine, l'endpoint répond **503** et le client reste en MJPEG. Côté navigateur : WebCodecs `VideoDecoder` quand disponible, repli MJPEG automatique (aucune frame décodée en 3 s ⇒ bascule + toast discret). *Pourquoi : item P2 n°5 de l'audit — bande passante ÷10 et 30-60 fps multi-clients, sans dépendre d'un encodeur GPL ni casser Safari (MJPEG conservé) ; le pire cas reste le comportement actuel.*
- **2026-07-28 — Soak 8 h scripté = gate de release.** Le rituel d'endurance fait à la main (audit P2 n°9) est formalisé dans `tools/soak.ps1` (GOTO en boucle + balayage master via WS, échantillonnage mémoire/threads/handles 30 s en CSV dans `docs/bench/`, verdict automatique PLATE/CROISSANTE/ECHEC, zéro process résiduel) et la checklist `docs/RELEASE.md` en fait une étape bloquante : pas de verdict PLATE, pas de release. *Pourquoi : la fiabilité 8 h est l'argument produit — elle doit être prouvée par version, pas affirmée.*
- **2026-07-28 — REPORTÉ : présentation DXGI flip model (WGL_NV_DX_interop2).** Chantier dédié à prototyper plus tard (audit P2 n°7). *Pourquoi : gros risque d'intégration (interop GL/D3D sur 4 fenêtres) pour un gain déjà couvert à ~80 % par le pacing DwmFlush livré en P1, pour un coût 100 fois moindre — on ne touche pas à la présentation qui marche la veille d'une v1.*
- **2026-07-28 — REPORTÉ : priorité GPU D3DKMT (`D3DKMTSetProcessSchedulingPriorityClass`).** Seul le volet process (SetPriorityClass ABOVE_NORMAL en mode Show) reste au backlog codable (P2 n°8). *Pourquoi : API semi-documentée, effets de bord possibles sur les autres process GPU d'une machine partagée (capture, OBS), bénéfice non mesuré sur nos scènes — MMCSS « Pro Audio » sur le thread de rendu est déjà en place et suffit tant qu'un bench ne prouve pas le contraire.*
- **2026-07-28 — REPORTÉ : télémétrie réseau (crash reporting type Sentry).** Aucune télémétrie dans le produit ; position consignée dans `docs/COMMERCIALISATION.md`. *Pourquoi : les minidumps sont des extraits mémoire — jamais d'envoi par défaut (audit P2 n°20) ; le besoin support est couvert localement par le rapport de diagnostic (zip local, chemins expurgés, envoi manuel par l'utilisateur) et les crash dumps WER documentés dans le manuel. Si un jour : opt-in strict, dialogue post-crash, envoi manuel.*
- **2026-07-28 — Vérification de mise à jour : opt-in strict, défaut DÉSACTIVÉ.** `update_check: bool` (défaut `false`) + `update_url` (défaut : `latest.json` à la racine du repo GitHub, servi en raw) ; une seule requête au démarrage, en mode Edit seulement, timeout 3 s, jamais de téléchargement — juste un badge `runtime.update {version, url, notes}`. *Pourquoi : audit P2 n°16 + cohérence avec la position télémétrie : rien ne sort de la machine sans geste volontaire.*
- **2026-07-28 — Clavier remappable : le moteur persiste, l'UI exécute.** `PatchTable.keys: Vec<KeyBinding {key, command}>` (serde default vide) + `EditOp::KeyBindingAdd/Remove` ; la capture de touche (mode learn) et la traduction touche→commande vivent dans la webui. Les raccourcis **système** — Espace (GO), Échap (panic), B (DBO) — restent prioritaires et non remappables. *Pourquoi : audit P2 n°4 (même mécanique que le MIDI learn) ; les organes de sécurité ne se remappent pas, c'est la doctrine « l'outil ne surprend jamais ».*

- **2026-07-29 — Chase timecode (MTC) livré, au niveau des CUES.** Nouveaux contrats : `core::TcTime` (« HH:MM:SS:FF », serde texte) + `TcRate` (24/25/29,97 DF/30), `Cue.triggers.timecode: Option<TcTime>` (serde default — shows antérieurs intacts), `ShowSettings.timecode_chase: bool` (défaut `false`), `runtime.timecode = {time, rate, locked, chasing}` (absent tant qu'aucun signal reçu). Réception MTC (quarter-frames + full-frame) dans `control-midi` sur le port d'entrée existant, horloge `MtcClock` drainée par le tick de session. Sémantique (référence QLab/média-serveurs) : avancée normale = GO des cues dont le déclencheur passe (transition respectée) ; saut avant/arrière ou re-verrouillage = GOTO de la dernière cue dont le déclencheur ≤ TC (noir si aucune) ; perte de signal = roue libre 2 s puis pause du chase — les cues actives CONTINUENT, rien n'est coupé. Cues avec et sans déclencheur coexistent. UI : toggle Réglages, colonne Timecode + badge TC dans Cues, affichage TC (vert/orange/gris) près de l'horloge, toasts lock/unlock. L'option « Timecode » du popover d'animation des paramètres RESTE grisée (`ModKind::TimecodeChase` réservé) : le chase pilote les cues, l'animation de paramètres au timecode viendra plus tard. *Pourquoi : option confirmée le 2026-07-23 ; le calage « dernière cue ≤ TC » est la convention métier attendue par les régisseurs, et la roue libre qui ne coupe rien suit la doctrine « l'outil ne surprend jamais ».*

## Bilingue FR/EN du 2026-08-05

- **2026-08-05 — Interface bilingue FR/EN livrée. Le FRANÇAIS est la langue SOURCE, et la chaîne française est la CLÉ du catalogue anglais.** Pas de fichiers de clés symboliques (`cue.delete.title`) : les libellés restent en clair dans `webui/app.js`, et `webui/i18n.js` porte un catalogue `{"<français>": "<anglais>"}` en JSON strict. *Pourquoi : (1) le code reste lisible — on voit ce que l'écran affiche sans ouvrir un second fichier ; (2) une chaîne oubliée s'affiche EN FRANÇAIS au lieu d'afficher une clé technique ou un vide — c'est la doctrine « chargement tolérant » appliquée à l'UI : en régie, un mot dans la mauvaise langue est un incident, un bouton vide est une panne ; (3) le français n'a aucune indirection au runtime (`tr(s)` renvoie `s`), donc zéro régression pour l'utilisateur principal.*
- **2026-08-05 — La traduction se fait à UN point de passage, pas à chaque site d'appel.** `appendChild` (tout texte construit par `el()`), les attributs `title`/`placeholder`/`aria-label`, et `data-tip` **au moment de l'affichage de l'infobulle** (ce qui couvre aussi celles écrites en dur dans `index.html`). Les messages qui mêlent texte et valeurs passent par `trf('… {0} …', valeur)` — jamais par concaténation. *Pourquoi : un `t()` à chaque site d'appel, c'est 500 occasions d'en oublier un, et un diff illisible ; le point de passage rend l'oubli structurellement impossible pour tout ce qui passe par `el()`. Le gabarit `trf` est indispensable parce que l'ordre des mots change d'une langue à l'autre — « Supprimer la cue 12 ? » / « Delete cue 12? » — ce qu'une concaténation ne peut pas rendre.*
- **2026-08-05 — Les avertissements du moteur (`runtime.warnings`) deviennent structurés : `{level, msg, key, args, action?}`.** `msg` reste la phrase française composée (compatibilité, journal, rapport de diagnostic) ; `key` est un gabarit de `core::warnings` (constante partagée) et `args` ses valeurs brutes (chemins, noms, messages système — jamais traduits). La web UI recompose `trf(key, …args)`, et retombe sur `msg` si `key` manque. *Pourquoi : le centre « État du show » est le texte MOTEUR le plus lu en régie (média manquant, moniteur perdu, port occupé) — le laisser en français dans une interface anglaise aurait vidé le bilinguisme de son sens. L'ajout est additif : un client ancien lit toujours `msg`.*
- **2026-08-05 — Périmètre assumé : l'interface est bilingue, le JOURNAL reste français.** Les lignes de `logs/` et de l'onglet Journal, ainsi que les toasts d'avertissement transitoires du moteur (`StateEvent::Warning`, ex. GO refusé par l'anti double-GO), restent en français. *Pourquoi : le journal est un outil de diagnostic destiné au support (donc à l'auteur, francophone), pas un organe de conduite ; le traduire imposerait de porter des clés dans chaque `warn!`/`error!` du moteur pour un lectorat quasi nul. Documenté dans le manuel plutôt que caché.*
- **2026-08-05 — Garde-fou en CI plutôt que discipline : `crates/control-http/tests/webui_i18n.rs`.** Le test scanne les littéraux de `app.js` (hors commentaires), les `data-tip` de `index.html` et les gabarits de `core::warnings`, retient ceux qui « font français » (accent ou mot outil), et ÉCHOUE si l'un n'a pas d'entrée au catalogue. Il vérifie aussi : aucune traduction vide, aucune entrée orpheline, les `{0}` conservés des deux côtés, aucune ponctuation française résiduelle en anglais (« », espace avant `:`), et aucun identifiant technique traduit par erreur (`cue-row`, `cue_add`, `cue/go`). Outil de maintenance associé : `cargo test -p conduite-control-http --test webui_i18n -- --ignored` écrit la liste exacte dans `target/i18n-fr-strings.json`. *Pourquoi : sans test, la deuxième langue pourrit en trois commits — c'est le mode d'échec classique de tout produit bilingue. Le scanner est volontairement LARGE (il ramasse quelques identifiants) : mieux vaut une entrée identité explicite qu'une chaîne qui passe entre les mailles.*
- **2026-08-05 — La langue vit dans le SHOW (`settings.language`), pas dans `config.toml`.** Bascule immédiate, sans rechargement (re-rendu complet : onglets, pied de page et badge de mode sont hors du panneau courant). *Pourquoi : le show voyage avec le spectacle — un show préparé en anglais s'ouvre en anglais chez le régisseur suivant, quelle que soit la machine. Le champ existait déjà et était inerte ; il est maintenant honoré.*

- **2026-08-05 — `CDLA-Permissive-2.0` ajoutée à l'allowlist de `deny.toml` (et de `about.toml`) pour `webpki-roots`.** La crate embarque le magasin de certificats racines de Mozilla, tiré par `ureq` (vérification de mise à jour, opt-in, désactivée par défaut) ; elle est passée sous Community Data License Agreement – Permissive 2.0, ce qui faisait rougir la CI `cargo deny`. *Pourquoi : ce n'est pas du code mais une DONNÉE, sous une licence de données permissive — aucune réciprocité, aucun copyleft, la seule obligation en redistribution binaire est de conserver les avis, déjà assurée par `licenses/THIRD-PARTY-NOTICES.html` généré par cargo-about. Compatible avec un produit vendu sous MIT. La règle « toute dépendance copyleft est bloquée et discutée avant d'entrer dans l'arbre » n'est pas assouplie : c'est exactement la discussion.*

## En attente / à trancher plus tard
- Confirmer l'interprétation du « module de synthétisation du son » (LFO + audio-réactif, sans sortie son).
- Machine(s) de show réelles pour le bench 4×1080p / 4K.
- Traduire aussi le journal et les toasts d'avertissement du moteur (exigerait des clés dans chaque `warn!` du moteur) — non retenu en v1.
